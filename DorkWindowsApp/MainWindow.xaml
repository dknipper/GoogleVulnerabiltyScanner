<Window x:Class="DorkWindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:commands="clr-namespace:DorkWindowsApp.Commands"
        mc:Ignorable="d"
        Title="MainWindow" WindowState="Maximized">
    <Window.CommandBindings>
        <CommandBinding Command="NavigationCommands.BrowseBack" CanExecute="BrowseBack_CanExecute" Executed="BrowseBack_Executed" />
        <CommandBinding Command="NavigationCommands.BrowseForward" CanExecute="BrowseForward_CanExecute" Executed="BrowseForward_Executed" />
        <CommandBinding Command="NavigationCommands.GoToPage" CanExecute="GoToPage_CanExecute" Executed="GoToPage_Executed" />
        <CommandBinding Command="commands:CustomCommands.Exit" CanExecute="ExitCommandCanExecute" Executed="ExitCommandExecuted" />
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Menu DockPanel.Dock="Top" Grid.Row="0" Grid.Column="0">
            <MenuItem Header="_File">
                <MenuItem Command="commands:CustomCommands.Exit" />
            </MenuItem>
        </Menu>
        <TabControl Grid.Row="1" Grid.Column="0">
            <TabItem Header="Browse Dorks">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Column="0" Grid.Row="0">
                        <Label>Keywords:</Label>
                        <TextBox Text="{Binding Path=Keywords, UpdateSourceTrigger=PropertyChanged}" />
                        <Label>Site:</Label>
                        <TextBox Text="{Binding Path=SiteToSearch, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <Grid Grid.Column="0" Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Name="Rect" VerticalAlignment="Stretch" Visibility="Collapsed">
                            <TreeView Name="Tv" Width="500" Height="{Binding ElementName=WbSample,Path=ActualHeight }" ItemsSource="{Binding GoogleDorkParentViewModels}">
                                <TreeView.Resources>
                                    <DataTemplate x:Key="ChildrenDataTemplate">
                                        <TextBlock Text="{Binding Path=GoogleUrl}" />
                                    </DataTemplate>
                                </TreeView.Resources>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Path=GoogleDorks}" ItemTemplate="{StaticResource ResourceKey=ChildrenDataTemplate}">
                                        <Border BorderBrush="BurlyWood" BorderThickness="2">
                                            <TextBlock Text="{Binding Path=Name}" />
                                        </Border>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="IsSelected" Value="{Binding Path=IsSelected}" />
                                        <Setter Property="IsExpanded" Value="{Binding Path=IsExpanded}" />
                                    </Style>
                                </TreeView.ItemContainerStyle>
                            </TreeView>
                        </Grid>
                        <Button Grid.Column="1" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Width="32" Height="32" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Right" Background="White" Click="Button_Click">&gt;</Button>
                    </Grid>
                    <Grid Grid.Column="1" Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <ToolBar Grid.Row="0">
                            <Button Command="NavigationCommands.BrowseBack">
                                &lt;
                            </Button>
                            <Button Command="NavigationCommands.BrowseForward">
                                &gt;
                            </Button>
                            <Separator />
                            <TextBox Name="TxtUrl" Width="300" KeyUp="txtUrl_KeyUp" />
                            <Button Command="NavigationCommands.GoToPage">
                                X
                            </Button>
                        </ToolBar>
                        <WebBrowser Grid.Row="1" Name="WbSample" Source="http://google.com/"></WebBrowser>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Sync Dorks" />
        </TabControl>
    </Grid>
</Window>
